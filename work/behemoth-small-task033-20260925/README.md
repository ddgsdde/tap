# 巨兽第 33 题：两个有效降本候选

目标题目是 **第 33 题（srcId 34），4 位无符号加法器，带 cin**。输入 9 位，输出 5 位，函数为 `A + B + Cin`。目标处理器为 Behemoth：`0x1F5Cb4aeaE1807Bf60c3b9C0D8aDBCC14e91f12C`。

## 比较结果

比较基线来自仓库快照区块 **123888327**，时间 **2026-09-25T04:53:48Z**。巨兽榜首电路 **2025** 为 49 个 NAND、深度 8、成本 25,088。

| 方案 | NAND | 深度 | 成本 N×D³ | 相对巨兽快照 |
|---|---:|---:|---:|---:|
| 巨兽基线 | 49 | 8 | 25,088 | 基线 |
| 本轮独立生成 | **48** | 8 | **24,576** | 降低 **2.040816%** |
| TapeOut 成果复用 | 53 | **7** | **18,179** | 降低 **27.539063%** |

**按总成本，53 门、7 层版本更强。** 48 门版本更省晶体管，但不是这两份中的最低成本方案。两份都是 NAND-only，均无 REF、无 LATCH。

这是相对固定快照的有效改进，不是已占据实时链上榜首，也没有数学全局最优证明。未提交链上交易。提交前仍须读取实时巨兽榜首确认候选成本是否更低。

## 来源说明

`task033_FRESH_48g8d_VERIFIED.json` 从加法关系、进位分组和之前从真值函数生成的进位模块出发，未读取榜首网表作为起点。完整生成电路先达到 51 门、8 层，再经保持完整功能正确的改写达到 50、49、48 门。保存了包含中性改写在内的 15 步确定性复现记录。独立生成不等于宣称全球首次发现。

`task033_TRANSFER_53g7d_VERIFIED.json` 直接复用已有 TapeOut 电路 **13234**，来源是 `data/latest/netlists/tapeout/task-033-circuit-13234.json`。它的原始网表完全未改，因此明确不宣称新原创结构。比较处理器改变为巨兽，成本仍按相同的 NAND 数和逻辑深度计算。

## 验证

两份候选均通过全部 **16×16×2 = 512** 组输入的独立标量验证，直接与整数 `A+B+Cin` 比较。独立 SMT 算术差异检查均返回 UNSAT，表示不存在输出反例；这不是最小门数证明。两个 BLIF 导出也分别重新解析并通过全部输入。

对两份电路分别故意破坏最低输出位后，标量验证和 SMT 验证都能拒绝错误结果。没有把部分输入通过或超时当作有效解。

在本目录中执行：

```bash
python3 verify.py task033_FRESH_48g8d_VERIFIED.json
python3 verify.py task033_TRANSFER_53g7d_VERIFIED.json
```

`verify.py` 仅依赖 Python 标准库，不信任 JSON 内附的验证结果。信号 0/1 为常量，2..5 为 A 的低位到高位，6..9 为 B 的低位到高位，10 为 Cin；最后五个物理 NAND 门依次为 S0、S1、S2、S3、Cout。

## 复现与实验包

仓库目录保存两个候选、独立验证器和本说明。完整对话 ZIP 另含 HEX、BLIF、全部搜索源码与日志、输入来源副本、SMT 文件、结果报告、算术配置和复现记录。候选中提到的 `reproduce48.py` 位于完整 ZIP 中。

在解压后的完整实验目录执行：

```bash
python3 reproduce48.py --out reproduced48.json
python3 verify.py reproduced48.json
```

生成器复现使用 Python 和 NumPy；SMT 检查使用系统 libz3。基本的网表全输入验证不需要这些额外依赖。

48 门网表原始字节 SHA-256：
`1d1f5fbf7a1080f2fb470a511a966a0769ecd7229213a6aad09be759ff887296`

53 门网表原始字节 SHA-256：
`8caa263e5c816b6fb40a0c8ed1cdc29378f1ad0492d659e961c5426b5b2fb023`

两份正确输出真值表 SHA-256 相同：
`44b2c0e540a151ff44ac2b659cf2b70265450d469210c213609f240229d19bf0`

所有搜索已经结束。本轮没有修改 data/latest、工作流或已有工作目录。
