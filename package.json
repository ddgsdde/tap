{
  "name": "tapeout-chain-data",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Reproducible TapeOut Protocol on-chain leaderboards, netlists and market snapshots",
  "scripts": {
    "scan": "node scripts/scan.mjs",
    "validate": "node scripts/validate.mjs",
    "sat:build": "sh tools/offline/build.sh",
    "sat:selftest": "sh tools/offline/selftest.sh"
  },
  "dependencies": {
    "ethers": "6.17.0"
  },
  "engines": {
    "node": ">=20"
  }
}
